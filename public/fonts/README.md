# Typography Configuration

The project utilizes two primary high-fashion editorial typography families:
- **Kanit** (weights 300 to 900) loaded via Google Fonts CDN in `index.html` and `src/styles/index.css`.
- **Inter** (weights 300 to 800) for high-legibility body prose and metadata.
- Monospaced numerals & indicators via system monospace font stack.
